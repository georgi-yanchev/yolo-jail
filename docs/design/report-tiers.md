---
title: "Report tiers — graduated to the reference tree"
date: 2026-09-10
status: accepted
tags: [design, cli, ux, host-apply, launch, reporting, graduated]
summary: "A stub. The report-tier design is built and its as-built account is docs/reference/report-tiers.md — the four tiers, the verdict contract, the remedy contract, the report vocabulary, the launch stream's rules and every OQ-RO ruling live there. This filename survives only while inbound references are repointed."
---

# Report tiers — graduated to the reference tree

**Status:** GRADUATED, 2026-09-13 — the settled body of this doc moved to
[`../reference/report-tiers.md`](../reference/report-tiers.md). Nothing is owed here.

> [!IMPORTANT]
> **The design is BUILT, and this file is no longer where it is described.**
> [`../reference/report-tiers.md`](../reference/report-tiers.md) is the as-built account: the
> four report tiers and what each class of line is for; P1–P8, including **P4, the rule that a
> launch has no quiet mode**, which [`../../AGENTS.md`](../../AGENTS.md) cites as law; the
> verdict contract and its six outcome tokens; the remedy contract; the closed report
> vocabulary; the launch stream under the same tiers; the machine-consumer boundary; the
> dependency rule; and the `## Why it's this way` appendix where every settled `OQ-RO` id
> resolves, [`OQ-RO3`](../reference/report-tiers.md#why-its-this-way) among them.
>
> **This stub carries no live question** — all seven were ruled on 2026-09-11 and every one is
> built. It is not the shape
> [`composed-file-permissions.md`](composed-file-permissions.md) and
> [`loophole-packaging.md`](loophole-packaging.md) use, which hold questions a reference may
> not carry.

**Why it still exists.** Two inbound references — both
[`OQ-RO3`](../reference/report-tiers.md#why-its-this-way) citations in
[`../plans/handoff-macos-user-open-threads.md`](../plans/handoff-macos-user-open-threads.md),
which another workflow held when this graduation landed — could not be repointed in the same
commit. Point them at
[`../reference/report-tiers.md#why-its-this-way`](../reference/report-tiers.md#why-its-this-way)
and **delete this file**, which is what the graduation would otherwise have done here.

Every reference in the Go tree already points at the reference doc, as does every other doc.
